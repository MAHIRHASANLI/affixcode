import React from "react";
import styles from "./index.module.css";
type Props = {};

const MessageItem = (props: Props) => {
  return <div className={styles["message-item-wrapper"]}>MessageItem</div>;
};

export default MessageItem;
