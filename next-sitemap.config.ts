import type { IConfig } from "next-sitemap";

const config: IConfig = {
  siteUrl: "https://affixcode.az",
  generateRobotsTxt: true, // (optional)
  // ...other options
  // exclude: ["/404", "/500"],
};

export default config;
